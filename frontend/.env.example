# Copy to `.env` or `.env.local` (not committed). Required at `npm run build` time — value is inlined into the bundle.
# Google Cloud → APIs & Services → Credentials → Maps Embed API key. Restrict by HTTP referrer.
VITE_GOOGLE_MAPS_EMBED_KEY=
